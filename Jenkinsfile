pipeline{
    agent any
    stages{
        stage("build"){
            steps{
                echo "--------build step---------------"
                sh 'ls'
                sh 'pwd'
            }
        }
        stage("deploy"){
            steps{
                echo "--------------------deploy step-----------------------------------"
            }
        }
        
    }
}
